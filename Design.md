# Design.md — 아침 브리핑 화면 디자인 기준서

화면 구성: 위에 날씨 상자 1개, 아래에 뉴스 카드 3개.

> 2026-09-20 개정: 라이트(애플·노션풍) → **프리미엄 다크 모드**로 확정.

---

## 1. 참고 스타일

깊이감 있는 다크 차콜 배경에 은은한 블루·퍼플 글로우를 더한, 세련된 프리미엄 대시보드 스타일.

---

## 2. 색

| 용도 | 헥사코드 | 설명 |
|---|---|---|
| 메인 배경 | `#0F1117` | 화면 전체 바탕, 깊이감 있는 다크 차콜 (좌상단·우상단에 은은한 블루/퍼플 radial glow) |
| 카드 배경 | `#1A1D24` | 날씨 상자·뉴스 카드의 바탕, 메인 배경보다 한 톤 밝게 |
| 글자 | `#F3F4F6` | 제목·본문 등 기본 텍스트, 다크 배경 위 고대비 화이트 |
| 보조 글자 | `#9AA3B2` | 시간·출처 등 부가 정보, 은은한 그레이 |
| 강조색 | `#5B8CFF` → `#9B6BFF` (그라디언트) | [경제] 태그, 링크 호버 등 강조 요소, 네온 블루~퍼플 |
| 테두리 | `#2D3748` | 카드 외곽선·구분선, 다크 배경 위에서도 경계가 은은하게 드러남 |

> 경고(기준 넘음) 배지 전용 색: 배경 `rgba(255,176,32,0.12)`, 글자 `#FFB84D`, 테두리 `rgba(255,176,32,0.35)` — 다크 옐로우/오렌지 톤.

---

## 3. 레이아웃

- **정렬 · 최대 폭**: 화면 중앙 정렬, 콘텐츠 최대 폭 960px
- **구역 순서**: 날씨 상자(전체 폭) → 뉴스 카드 3건 (구역 간 여백 32px)
- **뉴스 카드 배치**: 데스크톱(≥769px)은 가로 3열 그리드(`grid-template-columns: repeat(3, 1fr)`, gap 20px), 768px 이하 모바일에서는 자동으로 세로 1열 전환
- **카드 모양**
  - 모서리 둥글기: 날씨 상자 24px, 뉴스 카드 18px
  - 그림자: 날씨 상자 `0 20px 48px rgba(0,0,0,0.45)`, 뉴스 카드 `0 12px 28px rgba(0,0,0,0.35)` (짙고 부드럽게)
  - 안쪽 여백: 32px (날씨 상자), 22px (뉴스 카드)
  - 테두리: 1px solid `#2D3748`
  - 인터랙션: 뉴스 카드는 hover 시 4px 위로 살짝 떠오르며 테두리가 강조색으로 바뀜

---

## 4. 글자

- **글꼴**: Pretendard, 없을 경우 -apple-system, "Segoe UI", sans-serif

| 단계 | 크기 | 용도 |
|---|---|---|
| 큰 숫자 | 48~56px | 날씨 상자의 최고·최저기온·강수확률 등 핵심 수치 |
| 제목 | 24px | 날씨 상자 제목, 뉴스 섹션 제목 |
| 본문 | 18px | 뉴스 제목 한 줄, 경고 문구 (18px 이상 유지) |
| 보조 | 14px | 발행 시각, 출처, 라벨 등 부가 정보 |

---

## 5. 규칙 세 줄

1. 배경은 짙은 차콜(`#0F1117`), 카드는 한 톤 밝은 다크(`#1A1D24`)로 분리해 깊이감을 준다.
2. 화면은 최대 폭 960px 중앙 정렬, 날씨 상자 → 뉴스 3건(데스크톱 3열/모바일 1열) 순서를 지킨다.
3. 강조는 블루~퍼플 그라디언트, 경고는 옐로우/오렌지 톤으로만 쓰고 본문 글자는 18px 이상을 유지한다.
