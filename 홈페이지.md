홈페이지.md — 도구(Claude Code) 실행 절차서
안내: 이 문서는 도구(Claude Code)가 읽는 실행 절차서입니다. 사용자가 **"홈페이지.md의 N번 해 줘"**라고 요청하면 해당 번호의 절차를 순서대로 수행합니다. (대상 사용자: 비개발자)
📌 공통 규칙
* 보안 준수: .env 내의 열쇠 값(토큰, 비밀키 등)은 절대 화면에 출력하거나 다른 파일로 복사하지 않습니다.
* 사용자 친화적 보고:
   * 진행 상황은 짧고 쉬운 한국어로 전달합니다.
   * 명령어 원문은 사용자가 별도로 요청하지 않는 한 노출하지 않습니다.
   * 실패 시 원인 1줄, 다음 행동 가이드 1줄로 안내합니다.
* 사용자 개입 안내: 설치 확인창, 웹 로그인 등 사용자가 직접 클릭해야 하는 단계에서는 수행할 행동을 한 줄로 명확히 알립니다.
1번 · 준비물 설치 (git, gh)
1. 설치 여부 확인
2. Bash
git --version
gh --version
3. 4. 5. 미설치 프로그램 자동 설치
   * Git 미설치 시:
   * PowerShell
winget install --id Git.Git -e --source winget --accept-package-agreements --accept-source-agreements
   *    *    * GitHub CLI 미설치 시:
   * PowerShell
winget install --id GitHub.cli -e --source winget --accept-package-agreements --accept-source-agreements
   *    *    * 안내: 설치 중 Windows 허용 팝업이 뜨면 「예를 눌러 주세요」라고 알립니다.
6. 결과 보고
   * 하나라도 새로 설치한 경우:
「설치했어요. 앱을 완전히 닫았다가 다시 열고, 다시 1번을 시켜 주세요.」
   * 둘 다 이미 있는 경우:
「준비 완료 · git x.x · gh x.x」
   * winget 실행 불가 시:
「Windows가 오래돼 자동 설치가 안 돼요. git-scm.com 과 cli.github.com 에서 받아 설치한 뒤 다시 1번을 시켜 주세요.」
2번 · GitHub 로그인
   1. 로그인 상태 확인
   * gh auth status 실행. 이미 로그인되어 있다면 계정명만 보고하고 종료합니다.
   2. 새로 로그인 진행
   * 미로그인 시 실행:
   * Bash
gh auth login --hostname github.com --git-protocol https --web
   *    *    * 즉시 브라우저 실행 및 코드 안내:
   * start [https://github.com/login/device](https://github.com/login/device)
   * 안내 문구: 「브라우저에서 이 주소를 열고 코드 [XXXX-XXXX]를 넣은 뒤 Authorize를 눌러 주세요. 지금 기다리고 있어요.」
   3. 인증 연동 및 완료 보고
   * 브라우저 인증 완료 후 Git 자격증명 연동:
   * Bash
gh auth setup-git
   *    *    * 계정 확인 후 보고: 「<아이디>로 로그인됐어요.」
   * 시간 초과/취소 실패 시:
「코드를 넣기 전에 시간이 지났어요. 다시 2번을 시켜 주시고, 코드가 나오면 바로 넣어 주세요.」
3번 · 파일 올리기 (저장소 생성 및 Push)
      1. 보안 파일 제외 (.gitignore 설정)
      * 작업 폴더에 .gitignore 생성 후 아래 항목 추가:
      * Plaintext
.env
*.jpg
*.jpeg
*.png
*.webp
token.txt
      *       *       2. 배포 파일 준비
      * dashboard.html을 index.html로 복사 (덮어쓰기).
      3. 로컬 저장소 초기화 및 커밋
      * Git 저장소가 아니면: git init -b main
      * Git 설정 누락 시 설정:
      * git config user.name "<gh 계정명>"
      * git config user.email "<아이디>@users.noreply.github.com"
      * 커밋:
      * Bash
git add -A
git commit -m "morningbox: 첫 올리기"
      *       *       4. ⚠️ 사용자 최종 승인 (필수 대기)
      * 공개 저장소 생성 전 사용자에게 공개 대상 파일 목록을 표시합니다.
      * .env 및 이미지 파일이 포함되지 않았는지 자체 점검 후, 사용자에게 「공개로 올려도 돼」라는 답변을 받아야만 다음 단계로 진행합니다.
      5. 원격 저장소 생성 및 푸시
      6. Bash
gh repo create morningbox --public --source . --remote origin --push
      7.       * 이름 중복(오류) 시: morningbox2로 재시도하고 사용자에게 이름 변경 사실을 고지합니다.
      8. 보안 재검증
      * 원격 파일 목록 조회:
      * Bash
gh api repos/{owner}/{repo}/contents --jq ".[].name"
      *       *       * .env 노출 시 즉시 조치:
      * Bash
git rm --cached .env
git commit -m "보안: .env 제거"
git push
      *       * 사용자 경고: 「텔레그램 BotFather에서 /revoke로 열쇠를 새로 받아 .env를 고쳐 주세요. 한 번 올라간 열쇠는 지워도 안전하지 않아요.」
      * 정상이면 보고: 「[https://github.com/](https://github.com/)<아이디>/morningbox 에 올렸어요 · .env 없음 확인」
4번 · 웹사이트 배포 (GitHub Pages 활성화)
      1. Pages 활성화 API 호출
      2. Bash
gh api -X POST repos/{owner}/{repo}/pages -f build_type=legacy -f "source[branch]=main" -f "source[path]=/"
      3.       * 409 오류(이미 활성화됨): 다음 단계로 바로 진행.
      * 권한/업그레이드 오류: 저장소가 비공개일 가능성이 높으므로 공개 전환 후 재시도:
      * Bash
gh repo edit --visibility public --accept-visibility-change-consequences
      *       *       4. 주소 확인 및 접속 점검
      * 홈페이지 주소 추출:
      * Bash
gh api repos/{owner}/{repo}/pages --jq .html_url
      *       *       * 60~120초 대기 후 접속 확인.
      * 접속 성공 시: 「열림 확인」
      * 404 상태 지속 시: 「아직 만드는 중이에요. 2~3분 뒤 휴대폰에서 열어 보세요.」
      5. 브라우저 실행
      * 사용자에게 주소를 안내하고 실행: start <홈페이지주소>
5번 · 자동 갱신 스킬 등록
      1. 스킬 파일 생성 (누락된 경우)
      * .claude/skills/morning-board/SKILL.md가 없으면 신규 생성:
      1. 내용: ① 뉴스/날씨 정보 수집 → ② Design.md 기반 dashboard.html 생성 (열쇠 값은 직접 쓰지 않고 .env 참조 명시)
      2. 생성 후 안내: 「스킬이 없어서 새로 만들었어요.」
      2. 배포 단계 추가
      * SKILL.md 실행 절차 마지막에 아래 과정 추가:
      1. dashboard.html을 index.html로 복사
      2. 변경 사항 자동 커밋 & 푸시:
      3. Bash
git add -A
git commit -m "갱신"
git push
      4.       5. (변경 사항이 없으면 건너뜀)
      3. 최종 점검 및 완료 보고
      * SKILL.md 내부에 API 키/토큰 등 비밀값이 없는지 최종 확인.
      * 완료 보고: 「이제 /morning-board 를 실행하면 홈페이지도 같이 바뀌어요. PC와 앱이 켜져 있을 때만요.」